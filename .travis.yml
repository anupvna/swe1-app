language: python
python:
  - "3.9"

install:
  - pip install -r requirements.txt
  - pip install coverage 'coveralls[yaml]' flake8
script:
  - black --check .
  - flake8 .
  - coverage run manage.py test

after_success:
  - coveralls